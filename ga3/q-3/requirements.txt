fastapi>=0.128.8
openai>=2.20.0
uvicorn>=0.40.0
#Vercel does NOT need uvicorn in production runtime (it runs serverless), but keeping it is fine and helps in local testing.
pydantic>=2.0.0 #v2 is required for Literal and Field to work as used
python-dotenv>=1.0.0 #To load API keys from .env in deployment